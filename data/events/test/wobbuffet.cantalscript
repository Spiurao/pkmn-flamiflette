event loop()
{
    //Look at the character if enabled
    //in the event parameters
    if (inParameters("look"))
    {
        turnToFaceCharacter();
        wait(500);
    }
}

event actionPressed()
{
    //Walk 4 tiles up and 3 tiles
    //left if enabled in the event
    //parameters
    if (inParameters("walk"))
    {
        walk(UP, 4);
        walk(LEFT, 3);
        setParameters("look", true);
        setParameters("walk", false);
    }
}